{
  // 리터럴 타입 -> 값 자체를 타입으로 사용하는 것
  const num = 10;
  const str = "A";

  let num2: 5 = 5;
  num2 = 5;

  let bool: true = true;
  bool = true;

  const obj = { name: "sucoding " };
  obj.name = "kisu";
  const arr: number[] = [1, 2, 3];
}
